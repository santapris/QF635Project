# Binance trading configuration
# Copy this file to .env and fill in your values for local development.

# Environment selector — "dev" or "prod". Defaults to "dev".
ENVIRONMENT=dev

# API credentials — required for authenticated endpoints.
# If not set here, the loader will fall back to ~/vault/secret_file.txt .
BINANCE_API_KEY=
BINANCE_API_SECRET=
